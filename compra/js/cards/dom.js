// VARIABLES

let selected = null // con esto digo q no tengo valor, para no desestructurar los elementos y q los valores se guarden aca 


/** DOM
 * selecciono las tarjetas que tengo en pantalla 
 - primero selecciono el interior de la card
 - luego paso a agarar el card container que abarca todo junto
 * 
 */

const cards = document.getElementsByClassName('card-body')
const cardsContainer = document.querySelectorAll('.btn.card')





